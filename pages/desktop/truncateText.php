<div class="snippetheader">Input</div>

<div class="snippetcode">$text = "This is a long text that needs to be truncated.";
<br>$length = 20;
<br>$truncatedText = truncateText($text, $length);
</div>

<div class="snippetheader">Response in Code</div>

<div class="snippetresppnse">
   In this example, the input text "This is a long text that needs to be truncated." will be truncated to a length of 20 characters. 
   The output will be "This is a long text..." as the text is truncated and an ellipsis is appended to indicate the truncation.
</div>